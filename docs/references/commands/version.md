# `version`

Print the installed saifctl package version.

## Synopsis

```
saifctl version
```

## Description

Outputs the current saifctl package version to stdout. No arguments or flags are accepted. Equivalent to `saifctl --version`.

## Example

```sh
$ saifctl version
0.0.1
```

## Notes

- Produces no side effects.
