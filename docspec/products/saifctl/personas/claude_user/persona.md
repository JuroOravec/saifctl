# Claude user

Individual developer who already uses or evaluates **Claude** (Claude Code, claude-cli, or another Claude-based agent CLI) and wants a **safe** way to run it without risking their machine.

Often arrives via search (“run claude code safely”, “claude docker sandbox”, “sandbox claude”) rather than reading the full product docs linearly.
