---
prereq_concepts:
  - docker-isolation
arrival_context: search
search_terms:
  - run openclaw safely
  - openclaw docker sandbox
  - saifctl sandbox openclaw
user_stage: evaluating
---

# Task: protect my PC

The reader wants to run OpenClaw (or similar) so that the agent **cannot delete or modify** their real project until they explicitly choose to apply changes.

Success: they can run one clear command, understand what stays in the container vs the host, and know when to use `--extract`.
