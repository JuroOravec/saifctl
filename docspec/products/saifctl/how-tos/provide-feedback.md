---
persona: engineer
tasks:
  - steer-the-agent
goal: Add short, plain-language instructions to a run's task prompt that the agent picks up on the next inner round
---

How-to intent: walk through `saifctl run rules create <runId> --content "…"`, the live-vs-offline modes, when feedback arrives at the agent (next inner round, not mid-round), `--content-file` for longer rules, and the `list/get/update/rm` companion commands. Contrast with `inspect-and-start` (instructions vs direct code edits) and explain that the two can be combined.
