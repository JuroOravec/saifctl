---
source: src/sandbox-profiles/index.ts
type: config-schema
---

Sandbox profile inventory — predefined coder-container configurations covering language + package-manager combinations. Default: `node-pnpm-python`. Reference page lists every shipped profile, its base image, installed runtimes, override mechanism (`--profile <name>`, custom-profile via `--coder-image`), and how to define a project-local profile.
