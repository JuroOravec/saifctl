---
source: src/config/schema.ts
type: config-schema
---

Project-level saifctl configuration: `saifctl/config.{json,yaml,yml,ts}` stores defaults so you don't have to pass options on the CLI every time. Reference page documents every field, its type, default, and which CLI flag it corresponds to.
